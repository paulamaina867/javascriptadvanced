//create a js program that checks whether a number is positive or negative or zero
// read and make some short notes on comparison operators in   js logical operators assignment operators in js


let number = 2;

if (number > 0) {
    console.log("The number is positive.");
} else if (number < 0) {
    console.log("The number is negative.");
} else {
    console.log("The number is zero.");
}

//find out how to replace multiple ocurrences at once


let text = "The sky is blue. The ocean is also blue.";
let newText = text.replace(/blue/g, "green");

console.log(newText);
 
 //Replace multiple different words

 //.replace() calls
 

